{
  "name": "agente-nube",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agente de conocimiento en la nube: ingesta de leads, persistencia (Supabase + Google Sheets) y reportes diarios a dueños por WhatsApp (Elevator/GHL).",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "build": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  }
}
